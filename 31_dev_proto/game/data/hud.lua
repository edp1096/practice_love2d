-- game/data/hud.lua
-- HUD display configuration

return {
    -- Minimap
    minimap_enabled = true,  -- Show minimap by default (can be overridden by map properties)

    -- Health bar
    show_health_bar = true,

    -- Cooldowns
    show_cooldowns = true,
}
